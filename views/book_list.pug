extends layout

block content 
  h1= title
  if book_list.length
    ul.list-group
      each book in book_list
        li.list-group-item
          a(href=book.url class='text-dark') !{book.title} #{book.author.name}
  else
    p There is no books
